# Review: PR #59 / PR #60

Date: 2026-04-27
Repo: moremeds/xenon

## Verdict

- PR #59 is not safe to merge yet. `python-tests` failed in CI.
- PR #60 is mechanically mergeable: clean merge state and all listed CI checks green.
- PR #60 still has residual VCG share-card risk: `/vcg/share` runs `xenon-generate-vcg-share`, and `src/xenon/shares/generate_vcg_share.py` still reads `data/vcg.json` indirectly through `CACHE_PATH`. That is outside the History panel path, but it means the new guard test overstates the boundary.

## Findings

1. PR #59 blocks on CI.
   - GitHub reports `mergeStateStatus=UNSTABLE`.
   - `python-tests` failed.
   - Failure: affected pytest runner attempted to execute deleted `scripts/tests/test_incremental_sync.py`, producing `ERROR: file or directory not found: scripts/tests/test_incremental_sync.py`.
   - Likely fix: update `scripts/infra/dev/run_pytest_affected.py` to skip deleted test targets, or keep/replace the deleted test path until the runner handles deletions.

2. PR #60 VCG migration is green but incomplete if share behavior is in scope.
   - The History panel route now reads Postgres through FastAPI `/vcg`.
   - `/vcg/share` still delegates to `xenon-generate-vcg-share`.
   - `src/xenon/shares/generate_vcg_share.py` still reads `data/vcg.json` via `CACHE_PATH.read_text()`.
   - `scripts/tests/test_vcg_json_not_read.py` does not catch that indirect read because the literal `vcg.json` and `read_text()` occur on different lines.

## Checks Observed

- PR #59: web-typecheck, web-lint, web-tests, version-sync green; python-tests failed.
- PR #60: web-typecheck, web-lint, web-tests, python-tests, version-sync all green.

## Recommendation

- Merge PR #60 before PR #59 if you want the VCG History panel fix now, but treat VCG share-card migration as a follow-up unless you want the PR scope expanded.
- Do not merge PR #59 until the deleted-test CI failure is fixed and `python-tests` is green.
