# 3. Findings Table

Severity: Critical / High / Medium / Low.
Confidence: **Confirmed** (verified in code this review) / **Strong inference** (follows from
verified code, not exercised live) / **Hypothesis** (requires measurement or live test).

## 3.1 Order path

| ID    | Area             | Finding                                                                                                                                                                                                                                  | Evidence                                                                                                                        | Impact                                                                                                     | Severity                   | Confidence                                                    | Recommended action                                                                                                                     |
| ----- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | -------------------------- | ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| OP-1  | Order ack        | 15 s subprocess timeout (SIGKILL) after IB may have accepted the order is recorded as terminal `FAILED/SUBPROCESS_ERROR`; no ib ids persisted; UI shows failure; a user retry (new `client_attempt_id`) can place a duplicate live order | `server.py:2277-2286`; `subprocess.py:125-132`; blind sleep `ib_place_order.py:147-148`; path has zero route-level tests        | Duplicate live orders; orphaned broker state                                                               | **Critical**               | Confirmed (mechanism); duplicate occurrence: Strong inference | Introduce `UNCERTAIN` state on timeout + pass `client_attempt_id` as IB `orderRef` + reconciliation sweep (see 11-code-sketches §1,§5) |
| OP-2  | Persistence      | `mark_submitted` after broker ack has no error handling: DB failure → HTTP 500 with a live order and a row stuck `PENDING`                                                                                                               | `server.py:2325-2331`                                                                                                           | Live order invisible to the terminal until boot/poller                                                     | High                       | Confirmed                                                     | try/except → compensating `order_events` write + retry queue; alert                                                                    |
| OP-3  | Recovery         | No runtime sweep for stuck `PENDING` rows or `FAILED`-but-live orders; the 60 s `PENDING_TIMEOUT` runs only at boot rehydrate; poller never scans `PENDING`                                                                              | `single_leg_rehydrate.py:31,104-118`; `ib_activity_mirror.py:293-344`                                                           | Hung subprocess ⇒ row stuck until next restart; orphan re-imported only as a disconnected `snapshot-*` row | High                       | Confirmed                                                     | Add PENDING/UNCERTAIN scan to the poller tick, matching via `orderRef`                                                                 |
| OP-4  | State machine    | Successful `/orders/modify` never updates `limit_price`/`quantity` on Xenon-authored rows (only `modify_sequence`); drift-mirror explicitly skips UUID rows                                                                              | `orders_store.py:195-243,322-323`; `ib_order_manage.py` has zero `orders_store` references                                      | Postgres (UI, reports, audits) permanently shows pre-modify price                                          | High                       | Confirmed                                                     | `apply_modify` writes price/qty in the same UPDATE; add `MODIFY_APPLIED` event                                                         |
| OP-5  | State machine    | Naked-short audit cancels orders directly at IB, never calls `mark_terminal` — recurrence of incident #16 bug class; row stays WORKING up to 2 poll ticks and is then mislabeled `TWS_CANCEL_MIRROR`                                     | `naked_short_audit.py:285`; `ib_activity_mirror.py:115,266-273`                                                                 | Wrong audit provenance; stale open-order UI                                                                | High                       | Confirmed                                                     | Audit writes `mark_terminal(CANCELLED, reason_code=NAKED_SHORT_AUDIT)` + event                                                         |
| OP-6  | Combo replace    | Leg-structure modify = sequential cancel-then-place in the **Next.js route**; place failure after cancel leaves the position naked with only a "CRITICAL" error string                                                                   | `web/app/api/orders/modify/route.ts:105-204,176-184`                                                                            | Unprotected position under partial failure                                                                 | High                       | Confirmed                                                     | Move replace server-side; place-first-then-cancel where margin permits, or explicit two-phase with recovery record                     |
| OP-7  | Concurrency      | No bound on concurrent order subprocesses (9 unguarded call sites); cooldown fires only after failures                                                                                                                                   | `server.py:2277,2431,2604,...`; `server.py:3097-3143`; no `Semaphore` in file                                                   | N connections/processes under burst; clientId churn; gateway load                                          | Medium                     | Confirmed                                                     | `asyncio.Semaphore(2)` around order-mutating subprocess calls                                                                          |
| OP-8  | State machine    | `mark_terminal(expected_states=…)` optimistic guard used only by the TWS-cancel sweep; place-reject, cancel-route, boot-rehydrate callers can clobber a newer terminal state                                                             | `orders_store.py:536-551`; `ib_activity_mirror.py:259,272` (only users)                                                         | Terminal-state clobber races                                                                               | Medium                     | Confirmed                                                     | Pass `expected_states` at every terminal transition                                                                                    |
| OP-9  | Schema           | `order_submissions.state` is free text with no CHECK constraint (unlike `trades.state`)                                                                                                                                                  | `schema.py:576-632` vs `ck_trades_state` `schema.py:120`                                                                        | Arbitrary states representable; contradictory rows possible                                                | Medium                     | Confirmed                                                     | Add CHECK to the known state set (incl. new `UNCERTAIN`)                                                                               |
| OP-10 | Reconciliation   | Fills path runs on non-master clientIds (3/21); `reqExecutions`/`fills()` likely can't see TWS/mobile fills — contradicting `ib_activity_mirror.py`'s own docstring                                                                      | `ib_client.py:97`; `ib_reconcile.py:47-55`; docstring `ib_activity_mirror.py:1-9`; memory `project_ib_external_fills_invisible` | External fills never reach `order_fills`/blotter                                                           | Medium (High if confirmed) | Strong inference — verify on paper account                    | Live paper test; if confirmed, run fills mirror on clientId 0 or Flex overlay                                                          |
| OP-11 | Latency/ack      | Placement ack is a blind 2 s/5 s sleep, not event-driven; `initialStatus` is whatever arrived by then                                                                                                                                    | `ib_place_order.py:147-148`                                                                                                     | +2–5 s fixed latency; ambiguous status                                                                     | Medium                     | Confirmed                                                     | Await `openOrder`/`orderStatus` events with a deadline                                                                                 |
| OP-12 | Error mapping    | Place subprocess collapses all exceptions to a generic string (no classification), unlike manage's `classify_failure`                                                                                                                    | `ib_place_order.py:197-198` vs `ib_order_manage.py:32-57`                                                                       | Connection vs reject indistinguishable on the place path → wrong HTTP class                                | Medium                     | Confirmed                                                     | Reuse `classify_failure` in the place CLI                                                                                              |
| OP-13 | Dead code        | `pool_cancel_order`/`pool_modify_order` imported but never called — dormant, fully tested duplicate path                                                                                                                                 | `server.py:55` (only reference)                                                                                                 | Confusion; two maintained implementations                                                                  | Low                        | Confirmed                                                     | Decide: wire it (Option B) or delete it                                                                                                |
| OP-14 | Bypass           | `ib_execute.py` (564 lines) is a parallel place+monitor+persist path bypassing preflight/quote gate/idempotency; not covered by the caller allowlist                                                                                     | `ib_execute.py`; `order_path_caller_allowlist.py:38-52` guards only `ib_place_order`                                            | Operator tool can place unguarded orders                                                                   | Medium                     | Confirmed                                                     | Add to allowlist guard or make it call the FastAPI route; at minimum wire preflight                                                    |
| OP-15 | Docs-in-code     | `ib_pool.py` docstring claims `acquire_owner` serializes the cancel subprocess — false (only the naked-short audit uses it)                                                                                                              | `ib_pool.py:26-31` vs grep of `ib_order_manage.py`                                                                              | Future maintainers trust a wrong invariant                                                                 | Low                        | Confirmed                                                     | Fix docstring                                                                                                                          |
| OP-16 | Idempotency      | Dedup keys only `client_attempt_id`; no economic-terms duplicate detection (same contract+side+qty within seconds under a new id passes)                                                                                                 | `orders_store.py:106-160`                                                                                                       | User-driven duplicates (post-OP-1) unblocked                                                               | Medium                     | Confirmed (scope)                                             | Optional near-duplicate warning gate (soft 409 + override)                                                                             |
| OP-17 | Validation scope | Quote gate (freshness/tick/limit-band/market-hours) applies to **non-combo only**; combos rely on preflight + IB rejection                                                                                                               | `server.py:2185-2202`                                                                                                           | Combo fat-finger prices reach IB                                                                           | Medium                     | Confirmed                                                     | Extend limit-band sanity to combo net price                                                                                            |

## 3.2 Security / safety

| ID    | Area     | Finding                                                                                                                                                                                                                                              | Evidence                                                                            | Impact                                                                                            | Severity                                                               | Confidence                                            | Recommended action                                                                                                                                              |
| ----- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEC-1 | AuthZ    | Next.js middleware marks all `/api/(.*)` public; order routes have no auth check; `xenonFetch` self-authenticates to FastAPI with `X-Internal-Token` on every call — Clerk never gates order placement through the web proxy                         | `web/middleware.ts:12`; `web/lib/xenonApi.ts:27-44`; grep of order routes (no auth) | Anyone with network reach to the web port can place/cancel/modify orders                          | **High** (Critical if the web port is ever exposed beyond the tailnet) | Confirmed (mechanism); exposure: deployment-dependent | Require Clerk session in order-mutating Next routes (`auth.protect()` or explicit `getAuth` check) before proxying; keep internal token as transport trust only |
| SEC-2 | WS auth  | Relay upgrade bypasses ticket validation when `CLERK_JWKS_URL` unset **or** peer is loopback; no `Origin` validation; no rate limit on `/ws-ticket` or upgrades                                                                                      | relay `:440-453` (verified this review); `ws_ticket.py:17-45`                       | Cross-origin page on the operator's machine can attach to the quote stream; ticket-mint hammering | Medium                                                                 | Confirmed                                             | Add Origin allowlist; rate-limit ticket mint; keep loopback bypass only for server-to-server                                                                    |
| SEC-3 | Logging  | Unmasked IB account number logged at INFO; `mask_account` applied only to `/health`                                                                                                                                                                  | `server.py:524`; `guards.py:51-66`                                                  | Account id in log aggregation                                                                     | Low                                                                    | Confirmed                                             | Mask in logs                                                                                                                                                    |
| SEC-4 | Secrets  | Order subprocesses inherit the full parent env (DB URL, token secrets, Flex token)                                                                                                                                                                   | `subprocess.py:82-87` (no `env=`)                                                   | Broad blast radius if a CLI dep is compromised                                                    | Low                                                                    | Confirmed                                             | Pass a minimal env allowlist                                                                                                                                    |
| SEC-5 | Tickets  | In-process ticket store (single replica only); `/ws-ticket` mintable with the read-only `XENON_QUERY_API_KEY`                                                                                                                                        | `ws_ticket.py:17-31`; API-key path allowlist                                        | Horizontal scaling blocker; read-key grants stream access (arguably intended)                     | Low                                                                    | Confirmed                                             | Document; move to PG/redis only if replicas ever exist                                                                                                          |
| SEC-6 | Positive | No shell interpolation anywhere on the order path (`create_subprocess_exec` + argv lists); browser cannot select broker/account/mode on write routes; `validate_auth_config` refuses boot on token==key; read-only mode + dev/prod DB split verified | `subprocess.py:82-87`; `server.py:2140,2151`; `auth.py:230-243`; `dev.sh:133-138`   | —                                                                                                 | —                                                                      | Confirmed                                             | Preserve                                                                                                                                                        |

## 3.3 Quote stream

| ID    | Area          | Finding                                                                                                                                                                                                    | Evidence                                                                                             | Impact                                                                | Severity | Confidence                                                            | Recommended action                                                                   |
| ----- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | -------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| QS-1  | Backpressure  | No backpressure: bare `.send()` per client, no `bufferedAmount` check, unbounded per-client buffers; JSON serialized N× for N clients                                                                      | relay `:756-764,729-738,706`                                                                         | Slow client → unbounded memory/TCP backlog; CPU amplification         | High     | Confirmed                                                             | Bounded per-client queue + drop-oldest + `bufferedAmount` gate (11-code-sketches §7) |
| QS-2  | Testing       | The 2,256-line relay has **zero CI-run behavioral tests**: its probe is excluded (`norecursedirs`), the two `.mjs` unit tests never run in CI, and two Vitest "server" suites test local reimplementations | `pyproject.toml:112`; no workflow runs `.mjs`; `ws-server-keepalive/improvements.test.ts` headers    | Every reliability property of the quote path is unverified in CI      | High     | Confirmed                                                             | Extract testable core modules + spawn-relay integration test in CI                   |
| QS-3  | Protocol      | No sequence numbers; `timestamp` is relay receive time (except tape); no stale-overwrite protection beyond client heuristics                                                                               | `ib_tick_handler.js:179,285`; `pricesProtocol.ts` (no seq)                                           | Cannot detect reordering/staleness; order-entry prices of unknown age | Medium   | Confirmed                                                             | Add `seq` + `relay_ts` per message (11-code-sketches §6)                             |
| QS-4  | Subscriptions | Reconnect resubscribe is non-idempotent upstream: `startLiveSubscription` always cancels + re-issues `reqMktData`, per tab                                                                                 | relay `:833-862,1541-1546`; `usePrices.ts:712-714,825-826`                                           | IB churn on every tab refresh/reconnect                               | Medium   | Confirmed                                                             | "already live → attach subscriber only" fast path                                    |
| QS-5  | Frontend perf | Single `usePrices` in `WorkspaceShell`; `prices` prop feeds non-memoized `MetricCards` (1,309 ln) + `WorkspaceSections` (2,689 ln) → full-tree re-render up to every 100 ms                                | `WorkspaceShell.tsx:254,556-589`; no `React.memo` on either                                          | UI jank during volatility                                             | Medium   | Confirmed (structure); cost: Hypothesis — measure with React Profiler | Memoize consumers; per-symbol selector store (e.g. `useSyncExternalStore`)           |
| QS-6  | Recovery      | "Gateway restart" in docker/cloud mode only bounces the relay's own socket — cannot recover a wedged Gateway; only the non-docker branch shells to IBC restart                                             | relay `:665-689`                                                                                     | Watchdog can't fix the actual failure it detects in prod              | Medium   | Confirmed                                                             | Rename + add a real prod restart path (compose restart hook) or alert instead        |
| QS-7  | Capacity      | ~100 market-data lines per account shared across all gateway clients; relay has no error-101 branch; 500-symbol ambition is infeasible on IB                                                               | relay `:1855-1956` (no 101); memory `project_ib_marketdata_line_budget_shared` (measured previously) | Silent symbol starvation at scale                                     | Medium   | Strong inference (budget measured before; relay behavior confirmed)   | Handle 101 explicitly; cap + prioritize subscriptions                                |
| QS-8  | Duplication   | `usePrices` and `IBStatusContext` maintain two parallel sockets with duplicated reconnect/staleness/auth logic and copy-pasted constants                                                                   | `usePrices.ts:114-115` vs `IBStatusContext.tsx:55-56`                                                | Double connections; drift risk                                        | Low      | Confirmed                                                             | Shared WS-core hook; one socket, two channels                                        |
| QS-9  | Restart       | Relay state is all in-process; restart drops subscriptions/prices (clients rebuild via resubscribe)                                                                                                        | relay `:491-517,332-354`                                                                             | Warm-up gap after deploys                                             | Low      | Confirmed                                                             | Acceptable; document; snapshot-on-connect could smooth it                            |
| QS-10 | Duplication   | Depth/tape subsystem re-implements the entire batching/subscriber/cleanup pattern of L1 in the same file                                                                                                   | relay `:712-751` vs `:1231-1265`                                                                     | 2× maintenance of subtle logic                                        | Low      | Confirmed                                                             | Extract one generic batched-channel abstraction                                      |

## 3.4 Complexity, duplication, docs, tests

| ID   | Area            | Finding                                                                                                                                                                                                                                                                                                                                                                    | Evidence                                                                                                                    | Impact                                                             | Severity | Confidence                                        | Recommended action                                                                                                                      |
| ---- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | -------- | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| CX-1 | Duplication     | Naked-short/coverage math implemented 3× (TS guard, `preflight.py`, `naked_short_audit.py`) with comment-level parity; reason-code table mirrored by hand (`orderReasonCodes.ts` ↔ Python `ReasonCode`)                                                                                                                                                                    | `preflight.py:437-438` cites TS line numbers; `orderReasonCodes.ts:2-3`                                                     | Drift in a safety-critical rule                                    | High     | Confirmed                                         | One Python source of truth + parity fixtures/codegen for TS (keep TS as UX-only)                                                        |
| CX-2 | Module design   | `server.py` 3,248 ln / 25 endpoints / lifespan + inline services; `_orders_place_from_body` ~193 ln with 7 responsibilities                                                                                                                                                                                                                                                | `server.py:2139-2332`; def scan                                                                                             | Change amplification; test difficulty (why place path is untested) | Medium   | Confirmed                                         | Continue `routes/`+`services/` extraction; decompose place handler                                                                      |
| CX-3 | Dead code (web) | Regime gate deleted server-side (PR #104) but ~200 ln client retry logic ×2 forms + `regimeGate.ts` + stub modal remain; unified-order-system hooks/components dead (`useOrderPrices`, `useOrderValidation`, 4 components); combo net-price math ×3                                                                                                                        | `OrderTab.tsx:455-620,933-1110`; grep of `web/lib/order/`; `web/CLAUDE.md` combo section                                    | ~500+ lines of unreachable/duplicated UI logic                     | Medium   | Confirmed                                         | Delete regime client code + vestigial `regime_overrides` write path; finish or delete the unified order system; single combo-quote util |
| CX-4 | Docs            | `order-stack-end-to-end.md` (verified 2026-05-02) is materially wrong: documents the deleted regime gate as live, lists TWS-cancel mirroring (shipped 2026-06-14) as the top open gap, line numbers shifted ~750-900; three docs claim naked-short → HTTP 403 but code returns 400 (never accurate); README CLI table incomplete; `xenon-position-rules` CLI doesn't exist | drift-audit table (claims 1,3,11,25,26)                                                                                     | Stale doc misleads reviews and agents                              | Medium   | Confirmed                                         | Re-verify doc; fix 403→400 claims; regenerate CLI table                                                                                 |
| CX-5 | Duplication     | `cancel_order`/`modify_order` near-duplicate find/reconnect/poll-confirm loops                                                                                                                                                                                                                                                                                             | `ib_order_manage.py:184-277,280-447`                                                                                        | 2× maintenance                                                     | Low      | Confirmed                                         | Extract shared confirm-loop helper                                                                                                      |
| TS-1 | Tests           | Real-subprocess `/orders/place` path has zero route-level coverage (all place tests run test-mode); OP-1/OP-2/OP-12 branches all dark                                                                                                                                                                                                                                      | test-audit: every place route test sets `XENON_API_TEST_MODE=1`; `test_orders_routes_failures.py` covers only cancel/modify | The riskiest code path is unverified                               | High     | Confirmed                                         | Route tests with a fake place CLI (env-pointed stub binary) covering timeout/garbage/ack-then-kill                                      |
| TS-2 | Tests           | Nightly Playwright runs frontend-only (no FastAPI/IB/relay); 6/7 order specs intercept `/api/orders/*` in the browser, bypassing Next routes                                                                                                                                                                                                                               | `playwright.config.ts:26`; `nightly.yml`; spec table                                                                        | E2E label overstates coverage                                      | Medium   | Confirmed                                         | One nightly lane with fastapiHarness + stub CLIs                                                                                        |
| TS-3 | Tests           | Only `order-e2e.test.ts` exercises Next→FastAPI for real and it skips without `.venv`; CI execution of that lane not verified this review                                                                                                                                                                                                                                  | `fastapiHarness.ts:149-159`                                                                                                 | Possible silent skip in CI                                         | Medium   | Confirmed (skip mechanics); CI status: Hypothesis | Assert harness availability in CI (fail, don't skip)                                                                                    |
| FU-1 | Futu            | Execution absent by design; execution ledger schema-locked to IB (`broker='IB'` CHECKs); read mirror substantial and solid; positions path is a deliberate JSON-cache exception with a partial-failure guard                                                                                                                                                               | `schema.py:606,115,707,757`; capability matrix in 04 §4.2                                                                   | —                                                                  | Info     | Confirmed                                         | See Phase 5 decision (10-roadmap)                                                                                                       |

## 3.5 Failure-mode table (Part 10)

| Trigger                                               | Current behavior                                                                                                                                               | Risk                                   | Detectability                   | Recovery                                                 | Proposed improvement                                                             | Sev      | Likelihood               |
| ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------- | -------------------------------------------------------- | -------------------------------------------------------------------------------- | -------- | ------------------------ |
| IB Gateway down at place                              | Subprocess connect fails fast → 503 `IB_CONNECTION`; 15 s script cooldown engages                                                                              | Low — fail-closed                      | Good (toast + health)           | Automatic on gateway return                              | —                                                                                | Low      | Medium                   |
| Gateway drop mid-place (after IB accept)              | 15 s timeout → SIGKILL → row `FAILED`, no ib ids (OP-1)                                                                                                        | **Duplicate order on retry; orphan**   | Poor — looks like clean failure | Only boot rehydrate / disconnected snapshot row          | UNCERTAIN + orderRef reconciliation                                              | Critical | Low-Med                  |
| Ack timeout (slow gateway)                            | Same as above                                                                                                                                                  | Same                                   | Poor                            | Same                                                     | Same                                                                             | Critical | Medium                   |
| Broker accepted, FastAPI dies before `mark_submitted` | Row stuck `PENDING`; boot rehydrate → `FAILED/PENDING_TIMEOUT` after 60 s age; order live                                                                      | Orphan live order labeled FAILED       | Poor                            | Snapshot re-import as disconnected row                   | orderRef match in rehydrate                                                      | High     | Low                      |
| Duplicate retry, same `client_attempt_id`             | Reservation dedup: duplicate/terminal responses; no second subprocess (tested ×6-8 threads)                                                                    | None                                   | Good                            | —                                                        | Preserve                                                                         | —        | —                        |
| Duplicate retry, new attempt id                       | New order placed                                                                                                                                               | Duplicate exposure                     | None                            | Manual                                                   | Near-duplicate soft gate (OP-16)                                                 | High     | Medium after OP-1 events |
| DB down before place                                  | `reserve_attempt` raises → 500, nothing sent to broker                                                                                                         | Safe                                   | Good                            | Retry later                                              | —                                                                                | Low      | Low                      |
| DB down after broker ack                              | 500; live order; stuck PENDING (OP-2)                                                                                                                          | Orphan                                 | Poor                            | Boot/poller partial                                      | Compensating event + retry                                                       | High     | Low                      |
| Stale quote                                           | Non-combo: 503 `QUOTE_UNAVAILABLE` / fresh-snapshot fallback; combo: **no gate** (OP-17)                                                                       | Combo mispricing                       | Medium                          | —                                                        | Extend band check to combos                                                      | Medium   | Medium                   |
| Invalid contract                                      | Qualify fails → generic error (place CLI has no classifier, OP-12) → 502                                                                                       | Misclassified as reject                | Medium                          | —                                                        | classify in place CLI                                                            | Low      | Medium                   |
| IB pacing violation                                   | Falls into generic `ib_reject` bucket (no specific code handling)                                                                                              | Confusing UX                           | Medium                          | Retry                                                    | Map pacing codes                                                                 | Low      | Low                      |
| clientId collision                                    | Random-start probe + rotate on "in use"; exhaustion raises after 30 slots (tested)                                                                             | Low                                    | Good                            | Automatic                                                | —                                                                                | Low      | Low                      |
| Partial fill                                          | Rehydrate/poller mark `PARTIALLY_FILLED`; fill dedup by `exec_id` PK (tested)                                                                                  | Low                                    | 60 s lag                        | Automatic                                                | Push order events over WS to cut lag                                             | Low      | Medium                   |
| Late commission report                                | `update_fill_commission` idempotent patch + P&L re-aggregation (tested)                                                                                        | Low                                    | —                               | Automatic                                                | —                                                                                | Low      | Medium                   |
| External TWS modify                                   | `snapshot-*` rows: drift mirrored + event; **Xenon-authored rows: skipped** (OP-4 twin)                                                                        | Stale price/qty in PG for own orders   | Poor                            | —                                                        | Mirror drift onto UUID rows too (with sequence-safe rule)                        | Medium   | Medium                   |
| External TWS cancel                                   | Sweep marks CANCELLED after 2 ticks (grace); empty-snapshot guard defers when IB returns nothing                                                               | ≤2 min blindness; deliberate trade-off | Medium                          | Automatic                                                | —                                                                                | Low      | Medium                   |
| FastAPI restart                                       | Sequential boot: rehydrate → fills replay → poller (skipped in read-only/test)                                                                                 | Low                                    | Good                            | Automatic                                                | —                                                                                | Low      | —                        |
| Next.js restart                                       | Stateless proxy; in-flight requests dropped                                                                                                                    | Minimal                                | Good                            | Browser retry                                            | —                                                                                | Low      | —                        |
| Relay restart                                         | All subscriptions/prices dropped; port persisted; clients reconnect + resubscribe                                                                              | Warm-up gap; IB churn (QS-4)           | Good (status socket)            | Automatic                                                | Idempotent resubscribe                                                           | Low      | Medium                   |
| WS ticket expiry                                      | Fresh ticket minted per connect; single-use pop                                                                                                                | None                                   | —                               | Automatic                                                | —                                                                                | —        | —                        |
| Slow browser client                                   | Unbounded buffering; no drop (QS-1)                                                                                                                            | Relay memory/latency for all           | Poor (no metrics)               | Pong-timeout close after 65 s only if fully unresponsive | Bounded queues                                                                   | High     | Medium                   |
| Futu OpenD down                                       | Stale cache + warning; degraded-snapshot guard; `/health` shows last-sync state (can be stale-true)                                                            | Read-only surface only                 | Medium                          | Next sync                                                | —                                                                                | Low      | Medium                   |
| Two Xenon instances on one gateway                    | clientId ranges collide across processes (random probe mitigates); shared line budget starves symbols; two pollers double-mirror (register race no-op guarded) | Quote starvation; confusing mirrors    | Poor (error 101 swallowed)      | Manual (`lsof`)                                          | Handle 101 loudly; single-writer lock via `acquire_owner`-style PG advisory lock | Medium   | Known to happen (memory) |
